spring:
  application:
    name: licensing-service
  profiles:
    active: dev
  cloud:
    enabled: true
    config:
      uri: http://configserver:8071
      failFast: true
      retry:
        maxAttempts: 20
